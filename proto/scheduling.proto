syntax = "proto3";

package scheduling;

service SchedulingService {
    // unary
    rpc ScheduleInterview (InterviewInfo) returns (Empty);

    // server streaming
    rpc GetAllInterviews (Empty) returns (stream InterviewInfo);
}

message InterviewInfo {
    int32 candidateId = 1;
    string date = 2;
    string time = 3;
    string type = 4;
    string interviewer = 5;
    string status = 6;
}


message Empty {}

